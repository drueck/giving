<%= simple_form_for @contributor, :html => { :role => "form" } do |f| %>
  <%= f.input :first_name, label: 'First Name', input_html: { autofocus: 'autofocus' } %>
  <%= f.input :last_name, label: 'Last Name' %>
  <%= f.input :household_name, label: 'Household Name', input_html: { placeholder: 'Leave blank for default' } %>
  <%= f.input :address %>
  <%= f.input :city %>
  <%= f.input :state %>
  <%= f.input :zip %>
  <%= f.input :phone %>
  <%= f.input :email %>
  <%= f.input :notes, input_html: { rows: 4 } %>
  <div class="buttons">
    <%= f.button :submit, 'Save', :class => 'btn btn-default' %>
    <%= link_to 'Cancel', active_contributors_path, :class => 'btn btn-default' %>
    <% if @contributor.id %>
      <%= link_to 'Delete', active_contributor_path(@contributor),
        method: :delete,
        data: { confirm: 'Deleting this contributor will delete any associated contributions. Are you sure?' },
        class: 'btn btn-default' %>
    <% end %>
  </div>
<% end %>
