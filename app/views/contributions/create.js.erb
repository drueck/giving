$("#contributions-inline-form").html("<%= escape_javascript(render 'shared/inline_form'); %>");
$('.datepicker').datepicker({
  autoclose: true,
  format: 'mm/dd/yyyy',
  todayHighlight: true
});
$('#contributions').html("<%= escape_javascript(render("shared/contributions")) %>");
